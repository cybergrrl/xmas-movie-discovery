# Python Environment Setup

You should already be set up with a virtual environment as stated in the [project README](../README.md).

<!-- Install requirments for the web scripts from inside the "create web files" directory:

```
pip install -r requirements.txt
``` -->

## Idea

This is a script that takes as its input the final combined json file from the refinement scripts outputs and uses the data in there to build markup files that can be used in zola to build a website.